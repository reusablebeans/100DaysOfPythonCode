# generally bad practice to name variable ambiguously
n = "Areeb"
l = len(n)
print(l)

# variables should also be one unit (no spaces, although dashes and underscores can be used)
# variables can have numbers, but shouldn't begin with them
# variables should not be named after privilege words in python (print, input, etc.)

# this is a good example
username = "Areeb"
length = len(username)
print(length)